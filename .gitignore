# Local dev staging directory built by scripts/serve-local.ps1
_site/

# Local-only copy of data inside web/ (generated by serve-local script).
# In production GitHub Actions copies these into the deployed _site at
# build time; the canonical files live in data/.
web/data/

# Built collector binary
collector-bin
collector.exe
